[project]
name = "aws-test-harness"
version = "0.1.0"
description = "Infrastructure for test-driving real AWS resources from your local machine"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
dev-dependencies = [
    "boto3-stubs[cloudformation,stepfunctions]>=1.36.2",
    "boto3>=1.36.2",
    "pytest>=8.3.4",
]
